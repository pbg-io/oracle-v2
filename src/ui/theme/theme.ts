export type Theme = {
    colors: {
        buttonHover: string
        buttonText: string
        pageBg: string
        panelBg: string
        primary: string
    }
}

export const theme: Theme = {
    colors: {
        buttonHover: "#4d63ff",
        buttonText: "#fff",
        pageBg: "#e0e0e0",
        panelBg: "#fff",
        primary: "#0335ff"
    }
}
